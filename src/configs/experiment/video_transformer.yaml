defaults:
  - /model: video_transformer

model:
  pretrained: true
  depth: 4
  embed_dim: 256
  num_heads: 4
  dropout: 0.2
  head_hidden: 256

training:
  batch_size: 2
  lr: 0.0001
  epochs: 20
  checkpoint_path: ${hydra:runtime.cwd}/checkpoints/video_transformer_best.pt