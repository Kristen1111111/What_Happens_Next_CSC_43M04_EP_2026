defaults:
  - /model: video_transformer

model:
  pretrained: true
  embed_dim: 192
  depth: 3
  num_heads: 3
  dropout: 0.2
  head_hidden: 192

training:
  batch_size: 1
  lr: 0.0001
  epochs: 25
  checkpoint_path: ${hydra:runtime.cwd}/checkpoints/video_transformer_best.pt